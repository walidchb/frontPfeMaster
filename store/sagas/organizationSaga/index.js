import { put, call, takeEvery, select } from "redux-saga/effects";
// import getApi from '@/services/Api'
// import { _clearData, _storeData } from '@/helpers'

// let api
// ;(async function createAPI() {
//   api = await getApi()
// })()

function* organizationSaga() {
  // yield takeEvery("organization/GET_REGIONS", GET_REGIONS);
  // yield takeEvery("organization/SET_LANG", SET_LANG);
  console.log("organizationSaga");
}

export default organizationSaga;
