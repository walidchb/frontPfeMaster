import { put, call, takeEvery, select } from 'redux-saga/effects'
// import getApi from '@/services/Api'
// import { _clearData, _storeData } from '@/helpers'

// let api
// ;(async function createAPI() {
//   api = await getApi()
// })()

function* mainSaga() {
  // yield takeEvery("main/GET_REGIONS", GET_REGIONS);
  // yield takeEvery("main/SET_LANG", SET_LANG);
  console.log('mainSaga')
}

export default mainSaga
